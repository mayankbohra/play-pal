import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

// Render the App component into the "app" div
ReactDOM.render(<App name="Mayank"/>, document.getElementById('app'));
